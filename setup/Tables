
//session tickler view
SELECT
   DISTINCT a.*
FROM
   v_ADUSessionTickler a
INNER JOIN
   _sysUserPermissions b
ON
   a.SiteGUID = b.resource
WHERE
   b.requestor = '#session.userGUID#'
ORDER BY
   siteID,pid,acrostic



